<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="movies">
 	<select id="saleList" resultType="mybatis.vo.Movie" parameterType="java.util.Map">
 		SELECT *
 		FROM
 			TBL_MOVIE
 		<where>
	 		<if test="status!=null">
	 			 status=#{status}	
	 		</if>	
	 		<if test="mname!=null">
	 			<trim prefix="and">  <!-- 앞의 검색 조건 여부에 따라 and 추가  -->
					title=#{title}	 				
	 			</trim>
	 		</if>
 		</where>
 	</select>
 	<select id="getOne" parameterType="String" resultType="mybatis.vo.Movie">
 		SELECT *
 		FROM
 			TBL_MOVIE
 		WHERE mcode = #{mcode}
 	</select>
 </mapper>
 
 
 
 
 
 