<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="movies">
 
 		<select id = "selectAll" resultType="mybatis.vo.Movie">
 				SELECT *
 				FROM TBL_MOVIE
 		</select>
 		
 		 <!-- <select id = "selectUpcomingMovies" resultType="mybatis.vo.Movie" parameterType="String">
 				SELECT mname, poster
 				FROM TBL_MOVIE
 		</select>
 		
 		<select id = "selectEndMovies" resultType="mybatis.vo.Movie" parameterType="String">
 				SELECT mname, poster
 				FROM TBL_MOVIE
 		</select> -->
 		
 		<select id = "selectMovieById" parameterType = "long" resultType="mybatis.vo.Movie">
 				SELECT *
 				FROM TBL_MOVIE
 				WHERE mcode=#{mcode}
 		</select>
 		
 
 </mapper>
 
 
 
 
 
 